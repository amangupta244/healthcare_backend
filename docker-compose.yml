version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
      # MONGO_URI will be picked up from .env file
    networks:
      - fullstack-network

networks:
  fullstack-network:
    driver: bridge
